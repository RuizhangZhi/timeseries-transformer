{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "install-requirements",
      "type": "shell",
      "command": "python -m venv env && source env/bin/activate && python -m pip install --upgrade pip && if [ -f requirements.txt ]; then pip install -r requirements.txt; fi",
      "presentation": {
        "reveal": "always"
      },
      "runOptions": {
        "runOn": "folderOpen"
      }
    }
  ]
}
